# Frontend ↔ Go API Compatibility Contract Matrix

This matrix covers compatibility routes and recently reconciled frontend contracts;
it is not an exhaustive inventory of every core resource CRUD operation. The API
specification and generated Swagger remain authoritative for the complete API
surface. This matrix records integration and readiness state for compatibility
work. Update it when a caller, route, handler, flag, or smoke result changes.

Status vocabulary:

- **Backend implementation:** `Implemented`, `Partial`, `Feature-flagged`, or `Missing`.
- **Frontend integration:** `Integrated`, `Browser-only`, `Mock-only`, or `Not wired`.
- **Production readiness:** `Ready`, `Flagged/pending smoke`, or `Not ready`.
- Smoke status is deliberately dated. `Static contract smoke passed` means the
  gateway operation and generated Swagger method/path are both present;
  `seeded runtime blocked` means a live seeded request could not run in this
  environment (database/module-cache prerequisites were unavailable).

Rows dated before 2026-08-09 preserve historical runtime evidence from earlier
environments. They are not a substitute for rerunning the current seeded smoke
suite after contract changes.

The static and optional seeded checks are maintained by
`scripts/compatibility_smoke.py`; CI always runs the static check, while a
deployment environment can set `RUN_COMPATIBILITY_SMOKE=1`, `BASE_URL`, and
`ACCESS_TOKEN` for read-only seeded requests.

| Frontend caller | HTTP method/path | Go handler | Feature flag | Payload casing | Response | Backend implementation | Frontend integration | Production readiness | Smoke-test status/date |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Student roster page | `GET /students/roster` | `StudentHandler.Roster` | None | Query accepts `status`, `sortField`, `sortOrder` plus legacy `active`, `sort`, `order`; API body preserves admin row names | JSON `{data: {summary, filters, rows, pagination}}` | Implemented filters: search, class, status, gender, track, guardian, birth-year, pagination, and sort aliases; aggregate metadata remains partial | Integrated | Not ready for full dashboard fidelity: distribution/filter catalogs remain placeholders | Focused Go handler/repository tests passed — 2026-08-09; seeded GET smoke pending |
| Teacher roster page | `GET /teachers/roster` | `TeacherHandler.Roster` | None | Query accepts `status`, `sortField`, `sortOrder` plus legacy `active`, `sort`, `order`; API body preserves admin row names | JSON `{data: {summary, filters, rows, pagination}}` | Implemented filters: search, subject, track, availability, homeroom, status, pagination, and sort aliases; assignment/distribution metadata remains partial | Integrated | Not ready for full dashboard fidelity: assignment counts and distributions remain placeholders | Focused Go handler/repository tests passed — 2026-08-09; seeded GET smoke pending |
| Grade report page | `GET /grades/report` | `GradeHandler.Report` | None | Query camelCase; canonical write payloads are snake_case; status uses PASS/REMEDIAL/FAIL | JSON `{data: {context, summary, filters, rows, pagination}}` | Implemented filters, status predicates, enriched joins, sort aliases, and database-backed totals; filter catalogs and score distribution remain partial | Integrated | Not ready for full dashboard fidelity: metadata/distribution need aggregation queries | Focused Go handler/repository/service tests passed — 2026-08-09; seeded GET smoke pending |
| Generic grade edit | `PUT/PATCH /grades/:id` | `GradeHandler.Update` | None | Request is snake_case after data-provider conversion; legacy `score` is normalized to `grade_value` | JSON `{data: grade}` | Implemented compatibility alias | Integrated: generic data provider PUT and legacy PATCH both supported | Ready for compatibility contract; composite grade identity remains service-owned | Static contract and seeded PUT smoke passed — 2026-08-02 |
| Grade delete | `DELETE /grades/:id` | `GradeHandler.Delete` | None | Path ID; no request body | JSON `{data: {id, status: "deleted"}}` | Implemented soft delete (`deleted_at`) with final-grade recalculation | Integrated: grades page delete action | Ready for compatibility contract; authorization remains governed by RBAC | Static contract and seeded soft-delete smoke passed — 2026-08-02 |
| Grade component edit/delete | `PUT/DELETE /grade-components/:id` | `GradeComponentHandler.Update/Delete` | None | JSON `code`, `name`, optional `description`; code is preserved when omitted | JSON `{data: gradeComponent}`; delete returns `{data: {id, status: "deleted"}}` | Implemented soft delete (`deleted_at`) | Integrated: grade-component edit/delete resources | Ready for compatibility contract; authorization remains governed by RBAC | Static contract and seeded edit/delete smoke passed — 2026-08-02 |
| Student CSV import | `POST /students/import` | `StudentHandler.ImportCSV` | None | CSV headers: `nis,full_name,gender,birth_date,address,phone`; optional `Idempotency-Key` | JSON `{data: {created, failed, failures}}` | Implemented: 5 MiB/10,000-row limits, deterministic/keyed replay, duplicate row failures, audit record, best-effort row transactions | Integrated | Ready for compatibility contract; rate limiting is an operational concern | Static contract and seeded create/replay smoke passed — 2026-08-02 |
| Teacher CSV import | `POST /teachers/import` | `TeacherHandler.ImportCSV` | None | CSV headers: `email,full_name,nip,phone,expertise`; optional `Idempotency-Key` | JSON `{data: {created, failed, failures}}` | Implemented: 5 MiB/10,000-row limits, deterministic/keyed replay, duplicate row failures, audit record, best-effort row transactions | Integrated | Ready for compatibility contract; rate limiting is an operational concern | Static contract and seeded create/replay smoke passed — 2026-08-02 |
| Browser CSV compatibility export | `GET /export/students`, `/export/grades`, `/export/attendance` | `ExportCompatibilityHandler` | None | Direct snake_case CSV; students accept class/status/active/gender, grades accept class/subject/component/status, attendance accepts class/date range | Direct `text/csv` download, no JSON envelope | Implemented for documented filters; grade status uses PASS/REMEDIAL/FAIL with KKM 75 | Not wired: current UI uses local table exports; routes remain compatibility-only | Ready for filtered compatibility CSV; XLSX and browser wiring remain separate work | Focused static review — 2026-08-09; seeded CSV smoke pending |
| Attendance lesson editor | `POST /attendance`, `PUT/PATCH /attendance/:id` | `AttendanceHandler.LegacyUpsert` | `ENABLE_ATTENDANCE_ALIAS` | Request snake_case after data-provider conversion | JSON `{data: attendance}` | Feature-flagged | Integrated | Flagged; seeded compatibility writes passed with the route enabled | Static contract and seeded POST/PATCH smoke passed — 2026-08-02 |
| Teacher preferences editor | `POST /teacher-preferences`, `PUT /teacher-preferences/:id` | `TeacherPreferenceHandler.LegacyUpsert` | None | Request snake_case after data-provider conversion | JSON `{data: preference}` | Implemented | Integrated | Ready for compatibility contract; authorization remains governed by RBAC | Static contract and seeded POST/PUT smoke passed — 2026-08-02 |
| Exam events | `GET/POST/PUT/DELETE /exam-events` | `CalendarHandler` | None | Canonical snake_case | JSON envelope | Implemented compatibility alias | Integrated | Ready for compatibility contract; authorization remains governed by RBAC | Static contract and seeded CRUD smoke passed — 2026-08-02 |
| Enrollment edit | `PUT /enrollments/:id` | `EnrollmentHandler.Update` | None | `class_id` or legacy `target_class_id` | JSON `{data: enrollment}` | Implemented compatibility alias | Integrated | Ready for compatibility contract; transfer semantics are preserved | Static contract and seeded transfer/restore smoke passed — 2026-08-02 |
| Dashboard | `GET /dashboard`, `/dashboard/academics` | `DashboardHandler` | `ENABLE_DASHBOARD` + `ENABLE_ANALYTICS` | Query snake_case | JSON envelope | Feature-flagged | Integrated behind `VITE_ENABLE_DASHBOARD` + `VITE_ENABLE_ANALYTICS` | Flagged; seeded dashboard requests passed with the route enabled | Static contract and seeded GET smoke passed — 2026-08-02 |
| Analytics | `GET /analytics/*` | `AnalyticsHandler` | `ENABLE_ANALYTICS` | Query snake_case | JSON envelope | Feature-flagged | Integrated: `VITE_ENABLE_ANALYTICS` for dashboard + attendance analytics | Flagged; seeded attendance, grades, behavior, and system requests passed | Static contract and seeded GET smoke passed — 2026-08-02 |
| Reports page server jobs | `POST /reports/generate`, `GET /reports/status/:id`, `GET /export/:token` | `ReportHandler` | `ENABLE_REPORTS` | Request snake_case | JSON job status; tokenized CSV/PDF download | Feature-flagged | Integrated: reports page submits jobs and polls status | Flagged; seeded job reached FINISHED and CSV download returned 200 | Static contract and seeded job/status/download smoke passed — 2026-08-02 |
| Attendance analytics local CSV | No API request (browser `Blob` from loaded summaries) | None (browser-only) | `VITE_ENABLE_ATTENDANCE_ALIAS` page gate | Derived from loaded camelCase table rows | Local `text/csv` browser download | Not applicable | Browser-only: integrated in attendance analytics page | Not ready: export is limited to rows currently loaded in the browser | Browser-only path verified by static review; no seeded HTTP request — 2026-08-02 |
| Attendance analytics server aggregation | `GET /analytics/attendance` | `AnalyticsHandler.Attendance` | `ENABLE_ANALYTICS` | Query snake_case | JSON `{data: {summary, classBreakdown, weeklyTrend, studentDetails}}` | Feature-flagged | Integrated: uses `use-attendance-analytics-server` hook for server-side aggregation | Ready: eliminates 5000+ record client fetches; only fetches reference data + backend summaries | Static contract and seeded GET smoke passed — 2026-08-06 |
| Reports template selection | `POST /reports/generate` (template field) | `ReportHandler.GenerateReport` | `ENABLE_REPORTS` | Request snake_case with `template` field | JSON job status | Feature-flagged | Integrated: template selector in generate form | Ready: three layouts (simple, detailed, landscape) | Static contract smoke passed — 2026-08-06 |
| Schedule generator preview | `POST /schedules/generator` | `ScheduleGeneratorHandler.GenerateAlias` | `ENABLE_SCHEDULER` | Request snake_case with full payload | JSON `{mode, proposal: {proposalId, score, slots[], conflicts[], stats}}` | Feature-flagged | Integrated: uses canonical endpoint with conflict resolution UI | Ready: displays backend conflicts, stats, proposalId | Static contract and seeded POST smoke passed — 2026-08-06 |
| Schedule generator legacy | `POST /schedule/generate` | `ScheduleGeneratorHandler.Generate` | `ENABLE_SCHEDULER` | Request snake_case (minimal) | JSON `{mode, proposal}` | Feature-flagged (legacy) | Deprecated: use `/schedules/generator` | Not ready: legacy minimal payload | Static contract smoke passed — 2026-08-06 |
| Schedule save by proposal | `POST /schedule/save` | `ScheduleGeneratorHandler.Save` | `ENABLE_SCHEDULER` | Request snake_case with `proposalId` | JSON `{data: {scheduleId}}` | Feature-flagged | Integrated: saves via proposalId, no slot payload needed | Ready | Static contract and seeded POST smoke passed — 2026-08-06 |
| Auth token refresh/logout | `POST /auth/refresh`, `POST /auth/logout` | `AuthHandler.Refresh/Logout` | None | Refresh and logout request bodies use `refresh_token`; refresh response is common envelope `{data: {access_token, refresh_token, ...}}` | JSON envelope for refresh; 204 for logout | Implemented token rotation and revocation | Integrated: interceptor unwraps envelope and sends the required body; logout revocation covered by Go service test | Ready for client contract; seeded login → refresh → logout → refresh rejection remains to be run | Frontend contract tests + focused Go tests passed — 2026-08-09 |
| Feature discovery | `GET /features` | `FeatureHandler.List` | None | None | JSON `{apiPrefix, env, features: {...}}` | Implemented | Integrated: runtime discovery plus per-feature Vite fallback; explicit individual false overrides all-on | Ready: unauthenticated, build-time fallback | Focused frontend feature tests passed — 2026-08-09; seeded GET smoke pending |
| Mutations list | `GET /mutations` | `MutationHandler.List` | `ENABLE_MUTATIONS` | Query snake_case | JSON `{data: [...], total}` | Feature-flagged | Integrated: mutations page with filters | Ready | Static contract smoke passed — 2026-08-06 |
| Mutation approve/reject | `PATCH /mutations/:id/approve`, `PATCH /mutations/:id/reject` | `MutationHandler.Approve` / `MutationHandler.Reject` | `ENABLE_MUTATIONS` | Request snake_case with `comment` | JSON `{data: mutation}` | Feature-flagged | Integrated: approve/reject actions in UI with audit trail | Ready | Static contract smoke passed — 2026-08-06 |
| Archives list | `GET /archives` | `ArchiveHandler.List` | `ENABLE_ARCHIVES` | Query snake_case | JSON `{data: [...], total}` | Feature-flagged | Integrated: archives page with preview, download, upload | Ready | Static contract smoke passed — 2026-08-06 |
| Archive upload | `POST /archives/upload` | `ArchiveHandler.Upload` | `ENABLE_ARCHIVES` | multipart/form-data | JSON `{data: archive}` | Feature-flagged | Integrated: upload modal with file validation | Ready | Static contract smoke passed — 2026-08-06 |
| Archive download | `GET /archives/:id/download` | `ArchiveHandler.Download` | `ENABLE_ARCHIVES` | None | Signed URL redirect | Feature-flagged | Integrated: download action in UI | Ready | Static contract smoke passed — 2026-08-06 |

The canonical API spec is [GO_BACKEND_API_SPECIFICATION.md](GO_BACKEND_API_SPECIFICATION.md). The admin repository contains only a pointer to that document.
